- name: Include cleanup tasks
  include_tasks: "{{ item }}.yml"
  when: item in tasks
  loop: "{{ ansible_run_tags }}"
  vars:
    tasks:
      - nginx
      - modsecurity
      - kibana
      - moodle
      - odoo
      - nextcloud
      - bookstack
      - logstash
      - metricbeat
      - cadvisor
      - node-exporter
      - prometheus
      - fathom
      - elasticsearch
      - openldap
      - mysql
      - postgres
      - docker-network