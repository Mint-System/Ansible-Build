---
meilisearch_description: meilisearch
meilisearch_volume_name: "{{ meilisearch_hostname }}"
meilisearch_master_key: "{{ vault_meilisearch_master_key }}"
meilisearch_ports: []
meilisearch_network_mode: "{{ docker_network_name }}"
meilisearch_webhook: ""
meilisearch_etc_hosts: {}
