---
- name: Copy {{ role_name }} conf
  ansible.builtin.template:
    src: crowdsec-firewall-bouncer.yaml
    dest: /etc/crowdsec/bouncers/crowdsec-firewall-bouncer.yaml

- name: Install required {{ role_name }} packages
  ansible.builtin.include_role:
    name: packages
  vars:
    # apt_repos:
    #   - name: "https://packagecloud.io/crowdsec/crowdsec/ubuntu"
    #     branch: main
    #     key: "https://packagecloud.io/crowdsec/crowdsec/gpgkey"
    packages:
      - name: crowdsec-firewall-bouncer-iptables

# curl -fsSL https://packagecloud.io/crowdsec/crowdsec/gpgkey | gpg --dearmor > /etc/apt/keyrings/crowdsec_crowdsec-archive-keyring.gpg

# hx /etc/apt/sources.list.d/crowdsec_crowdsec.list

# deb [signed-by=/etc/apt/keyrings/crowdsec_crowdsec-archive-keyring.gpg] https://packagecloud.io/crowdsec/crowdsec/ubuntu jammy main
# deb-src [signed-by=/etc/apt/keyrings/crowdsec_crowdsec-archive-keyring.gpg] https://packagecloud.io/crowdsec/crowdsec/ubuntu jammy main
