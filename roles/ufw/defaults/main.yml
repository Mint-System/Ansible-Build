ufw_rules:
  default:
    - rule: "limit"
      port: "22"
      proto: "tcp"
    - rule: "allow"
      port: "80"
      proto: "tcp"
    - rule: "allow"
      port: "443"
      proto: "tcp"
ufw_profile: default