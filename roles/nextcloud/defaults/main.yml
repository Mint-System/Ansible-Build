---
nextcloud_description: Nextcloud
nextcloud_timezone: Europe/Zurich
nextcloud_volume_name: "{{ nextcloud_hostname }}"
nextcloud_trusted_domains: "{{ nextcloud_domain }}"
nextcloud_build_image: false
vault_nextcloud_postgres_password: "{{ vault_nextcloud_admin_password }}"
nextcloud_postgres_password: "{{ vault_nextcloud_postgres_password }}"
nextcloud_mysql_password: "{{ vault_nextcloud_mysql_password }}"
nextcloud_etc_hosts: {}
