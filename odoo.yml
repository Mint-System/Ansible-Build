- hosts: all
  become: true
  roles:
  - role: docker
    tags: docker
  - role: docker-network
    tags: docker-network
  - role: postgres
    tags: postgres
  - role: odoo
    tags: odoo
  - role: odoo-scripts
    tags: odoo-scripts
  - role: modsecurity
    tags: modsecurity
  - role: nginx
    tags: nginx